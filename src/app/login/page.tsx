import React from "react";
import FormLogin from "../components/auth/FormLogin";
export const metadata = {
  title: "Login - LATDA",
};
function page() {
  return <FormLogin />;
}

export default page;
